You should add a new page in the Accounts app (separate from the current target settings page) where the Accounts Head can input daily achieved sales in BDT against previously set monthly targets; this page should simply reuse your existing data structure (targets, departments, branches, or salesmen) and store daily entries without modifying any current logic. Then, calculate progress as (total achieved amount ÷ monthly target) × 100 and display it as a pie chart, matching the same visualization style used in your KPI dashboard in the admin app, ensuring you reuse the same chart library and design so the UI remains consistent. Finally, to deploy, open your project folder on your machine, review your AGENTS.md for any environment-specific instructions, run your existing deployment script using deploy.bat (or the equivalent command it contains), and make sure your server is configured to bind to 192.168.100.3 on port 4001 so the app becomes accessible at that address.